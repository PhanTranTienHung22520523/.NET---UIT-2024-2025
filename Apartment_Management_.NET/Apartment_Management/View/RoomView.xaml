<UserControl x:Class="Apartment_Management.View.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Apartment_Management.View"
             xmlns:vm="clr-namespace:Apartment_Management.ViewModel" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             Background="#151f2d">
    <UserControl.DataContext>
        <vm:Room_View_Model/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header Section -->

        <!-- Main Content Section -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Room Information Section -->
            <Border Background="#1e2e44" CornerRadius="10" Padding="10" Margin="10">
                <StackPanel>
                    <TextBlock Text="Room Information" FontSize="18" FontWeight="Bold" Margin="0 0 0 10" Foreground="White"/>

                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <TextBlock Text="Room Name: " Width="100" VerticalAlignment="Center" Foreground="White" FontSize="15" />
                        <TextBox Width="200"  Text="{Binding Room.RoomName}" IsReadOnly="True" FontSize="15"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <TextBlock Text="Price: " Width="100" VerticalAlignment="Center" Foreground="White" FontSize="15" />
                        <TextBox Width="200" Text="{Binding Room.Price}" FontSize="15"/>
                    </StackPanel>

                    <Button Content="Save Room Info" Background="#28B463" Foreground="White" Width="150" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <!-- Residents and Items Section -->
            <Grid Grid.Column="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Residents Section -->
                <Border Background="#1e2e44" CornerRadius="10" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Dweller" FontSize="18" FontWeight="Bold" Margin="0 0 0 10" Foreground="White" />
                        <DataGrid 
                  Background="#202c44" Foreground="#ffffff"
                  AutoGenerateColumns="False"
                  HorizontalScrollBarVisibility="Hidden"
                  VerticalScrollBarVisibility="Hidden"
                  GridLinesVisibility="Vertical"
ItemsSource="{Binding DwellerList }"                  
                  RowHeight="30"
                  FontSize="15"
                  IsReadOnly="True"
 HeadersVisibility="Column" CanUserAddRows="False"    CanUserResizeColumns="False" 
                 >
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#1e2e44" />
                                    <Setter Property="Foreground" Value="#9ea4ad" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="#243347"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns  >
                                <DataGridTextColumn Header="Tên" Binding="{Binding DwellerName }" Width="*" IsReadOnly="True" CanUserSort="True" />
                                <DataGridTextColumn Header="Số điện thoại" Binding="{Binding DwellerPhone}" Width="*" IsReadOnly="True" CanUserSort="True" />
                                <DataGridTextColumn Header="Email" Binding="{Binding DwellerEmail}" Width="*" IsReadOnly="True" CanUserSort="True"/>
                            </DataGrid.Columns>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <!--<Setter Property="Background" Value="#243347" />-->
                                    <!--<Setter Property="HorizontalAlignment" Value="Center"/>
         <Setter Property="VerticalAlignment" Value="Center"/>-->
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Background" Value="#243347" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderThickness" Value="0" />


                                    <!-- Căn giữa nội dung -->
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>

                                    <!-- Xử lý khi ô được chọn -->
                                    <Style.Triggers>
                                        <!-- Khi ô được chọn -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#243347" />
                                            <!-- Màu nền khi được chọn -->
                                            <Setter Property="Foreground" Value="White" />
                                            <!-- Màu chữ khi được chọn -->
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <!-- Header Style -->
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#243347" />
                                    <Setter Property="Foreground" Value="#ffffff" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="BorderBrush" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                    <Setter Property="Height" Value="30"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Items Section -->
                <Border Background="#1e2e44" CornerRadius="10" Padding="10" Margin="5" Grid.Row="1">
                    <StackPanel>
                        <TextBlock Text="Stuff" FontSize="18" FontWeight="Bold" Margin="0 0 0 10" Foreground="White" />

                        <DataGrid 
                  Background="#202c44" Foreground="#ffffff"
                  AutoGenerateColumns="False"
                  HorizontalScrollBarVisibility="Hidden"
                  VerticalScrollBarVisibility="Hidden"
                  GridLinesVisibility="Vertical"
ItemsSource="{Binding StuffList}"                
                  RowHeight="30"
                  FontSize="15"
                  IsReadOnly="True"
 HeadersVisibility="Column" CanUserAddRows="False"    CanUserResizeColumns="False" 
                 >
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#1e2e44" />
                                    <Setter Property="Foreground" Value="#9ea4ad" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="#243347"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns  >
                                <DataGridTextColumn Header="ID" Binding="{Binding StuffID}" Width="*" IsReadOnly="True" CanUserSort="True" />
                                <DataGridTextColumn Header="Tên" Binding="{Binding Stuff_Name}" Width="*" IsReadOnly="True" CanUserSort="True" />
                                <DataGridTextColumn Header="Số lượng" Binding="{Binding Stuff_Number}" Width="*" IsReadOnly="True" CanUserSort="True"/>
                             
                            </DataGrid.Columns>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <!--<Setter Property="Background" Value="#243347" />-->
                                    <!--<Setter Property="HorizontalAlignment" Value="Center"/>
         <Setter Property="VerticalAlignment" Value="Center"/>-->
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Background" Value="#243347" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="BorderThickness" Value="0" />


                                    <!-- Căn giữa nội dung -->
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>

                                    <!-- Xử lý khi ô được chọn -->
                                    <Style.Triggers>
                                        <!-- Khi ô được chọn -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#243347" />
                                            <!-- Màu nền khi được chọn -->
                                            <Setter Property="Foreground" Value="White" />
                                            <!-- Màu chữ khi được chọn -->
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <!-- Header Style -->
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#243347" />
                                    <Setter Property="Foreground" Value="#ffffff" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="BorderBrush" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                    <Setter Property="Height" Value="30"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
