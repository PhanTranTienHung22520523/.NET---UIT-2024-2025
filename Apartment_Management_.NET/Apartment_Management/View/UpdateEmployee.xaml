<Window x:Class="Apartment_Management.View.UpdateEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:vm="clr-namespace:Apartment_Management.ViewModel"
        xmlns:local="clr-namespace:Apartment_Management.View"
        mc:Ignorable="d"
        Title="AddEmployee" d:DesignHeight="500" d:DesignWidth="800">
    <Window.DataContext>
        <vm:Add_Employee_View_Model/>
    </Window.DataContext>
    <Grid Background="#151f2d" Margin="10">
        <TextBlock Text="Update Employee" FontSize="24" FontWeight="Bold" Foreground="White" 
            HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <Grid Margin="30,50,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>


            <TextBlock Text="Name:" Grid.Row="1" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <TextBox  Grid.Row="1" Grid.Column="1" Height="25" Margin="0,10" Text="{Binding EmployeeName}" />

            <TextBlock Text="Sex:" Grid.Row="2" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <ComboBox Grid.Row="2" Grid.Column="1"   SelectedValue="{Binding EmployeeSex}" SelectedValuePath="Content" Height="25" Margin="0,10">
                <ComboBoxItem Content="Male" />
                <ComboBoxItem Content="Female" />
            </ComboBox>
            <TextBlock Text="Role:" Grid.Row="3" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding EmployeeRole}"  Height="25" Margin="0,10" />

            <TextBlock Text="Password:" Grid.Row="4" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <TextBox  Grid.Row="4" Grid.Column="1" Height="25" Margin="0,10" Text="{Binding EmployeePassword}" />

            <TextBlock Text="Birthday" Grid.Row="5" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <DatePicker x:Name="BirthDatePicker" Grid.Row="5" Grid.Column="1" Height="25" Margin="0,10" SelectedDate="{Binding BirthDate, Mode=TwoWay}" />

            <TextBlock Text="Start Date:" Grid.Row="6" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <DatePicker x:Name="StartDatePicker" Grid.Row="6" Grid.Column="1" Height="25" Margin="0,10" SelectedDate="{Binding StartDate, Mode=TwoWay}" />

            <TextBlock Text="Phone:" Grid.Row="7" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <TextBox x:Name="PhoneTextBox" Grid.Row="7" Grid.Column="1" Height="25" Margin="0,10" Text="{Binding Phone}"/>

            <TextBlock Text="Email:" Grid.Row="8" Grid.Column="0" Margin="0,10,10,10" VerticalAlignment="Center" Foreground="White"/>
            <TextBox x:Name="EmailTextBox" Grid.Row="8" Grid.Column="1" Height="25" Margin="0,10"  Text="{Binding Email}"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" Grid.Row="9" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button  Width="100" Margin="10,0"  Background="#4CAF50" Foreground="White" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Icon:PackIconMaterial Kind="ContentSave" />
                </Button>
                <Button  Width="100" Margin="10,0"  Background="#F44336" Foreground="White" Command="{Binding DeleteCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Icon:PackIconMaterial Kind="Delete" />
                </Button>
                <Button  Width="100" Margin="10,0"  Background="#F44336" Foreground="White" Command="{Binding CancelCommand }" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Icon:PackIconMaterial Kind="Cancel" />
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
